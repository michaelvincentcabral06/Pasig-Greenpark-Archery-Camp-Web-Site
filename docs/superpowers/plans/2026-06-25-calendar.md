# On-brand Calendar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A custom on-brand month calendar (+ day detail) replacing the Google Calendar iframe — two modes: admin (Bookings tab, real sessions) and public (Programs page, availability only, no names).

**Architecture:** One SuperConductor component in `index.html` (mirrored to `Pasig Greenpark Archery Camp.dc.html`). A shared month-grid builder feeds both modes. Admin reads the already-loaded `allBookings`; public fetches the PII-free `?action=availability` per day-tap. View-only. No backend change, no deploy.

**Tech Stack:** SuperConductor template (`{{ }}`, `<sc-if>`, `<sc-for>`), plain class-component JS (modern JS OK), Playwright-core with stubbed `fetch`.

## Global Constraints

- **Mirror rule:** every `index.html` edit applied identically to `Pasig Greenpark Archery Camp.dc.html`; end with `diff index.html "Pasig Greenpark Archery Camp.dc.html" && echo IDENTICAL`.
- **No backend change, no deploy.** Admin uses `allBookings`; public uses `?action=availability` only.
- **Privacy:** the PUBLIC calendar must NEVER request or render `?action=bookings` (names) — only `?action=availability` (slot counts).
- **View-only:** no editing from the grid; actions stay in the Sessions list / booking flow.
- **Verification:** Playwright-core with stubbed `fetch`; chromium at `C:\Users\Michael Cabral\AppData\Local\ms-playwright\chromium-1228\chrome-win64\chrome.exe`; install if missing: `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm i playwright-core --no-save`. Scratch harness `_verify_cal.mjs` (deleted in the final task). 0 real console errors.

---

### Task 1: Shared month grid + Admin calendar (Bookings → Calendar)

**Files:**
- Modify: `index.html` — initial state (calendar keys); the shared grid builder + admin builders (render section); `bkView` three-state defs (~4666); render return; the Bookings-tab toggle + a Calendar view (markup ~1959-2007)
- Mirror: `Pasig Greenpark Archery Camp.dc.html`
- Test: `_verify_cal.mjs`

**Interfaces:**
- Consumes: `allBookings`, `coachById`, `sessStatusInfo` (the admin-render local ~4610), `todayStr`.
- Produces: shared `calGrid`/`calMonthLabel`/`calPrev`/`calNext`/`calSel`/`calSelLabel`/`calDowHeads`/`calHasSel`; admin `calAdminCells`/`calAdminDay`; `bkViewCalendar`/`goCalendar`/`bkCalTabBg`/`bkCalTabFg`. Task 2 reuses the shared grid.

- [ ] **Step 1: Add calendar state.** In the initial state (near `bkView: 'sessions',`):
```js
    calMonth: '', calSel: '', calDayAvail: [], calAvailLoading: false,
```

- [ ] **Step 2: Three-state `bkView`.** Replace `const bkViewSessions = this.state.bkView !== 'passes';` (~index.html:4666) with:
```js
    const bkViewSessions = (this.state.bkView || 'sessions') === 'sessions';
    const bkViewPasses = this.state.bkView === 'passes';
    const bkViewCalendar = this.state.bkView === 'calendar';
    const goCalendar = () => this.setState({ bkView: 'calendar' });
```

- [ ] **Step 3: Shared month-grid builder.** In the admin render section (near the other Bookings-tab builders, after `sessStatusInfo` ~4610), add:
```js
    const calCur = this.state.calMonth || this.todayStr().slice(0, 7);
    const calY = parseInt(calCur.slice(0, 4), 10), calM = parseInt(calCur.slice(5, 7), 10);
    const calFirstDow = new Date(calY, calM - 1, 1).getDay();
    const calDaysInMonth = new Date(calY, calM, 0).getDate();
    const calTodayStr = this.todayStr();
    const calPad2 = (n) => ('0' + n).slice(-2);
    const calGrid = [];
    for (let i = 0; i < calFirstDow; i++) calGrid.push({ blank: true });
    for (let d = 1; d <= calDaysInMonth; d++) {
      const ds = calY + '-' + calPad2(calM) + '-' + calPad2(d);
      calGrid.push({ blank: false, date: ds, day: d, isToday: ds === calTodayStr, selected: ds === this.state.calSel });
    }
    const calMonthLabel = new Date(calY, calM - 1, 1).toLocaleDateString('en-PH', { month: 'long', year: 'numeric' });
    const calShift = (delta) => { let y = calY, m = calM + delta; if (m < 1) { m = 12; y--; } if (m > 12) { m = 1; y++; } this.setState({ calMonth: y + '-' + calPad2(m), calSel: '', calDayAvail: [] }); };
    const calPrev = () => calShift(-1), calNext = () => calShift(1);
    const calSelLabel = this.state.calSel ? (() => { try { return new Date(this.state.calSel + 'T00:00:00').toLocaleDateString('en-PH', { weekday: 'long', month: 'long', day: 'numeric' }); } catch (e) { return this.state.calSel; } })() : '';
    const calDowHeads = ['Su', 'Mo', 'Tu', 'We', 'Th', 'Fr', 'Sa'];
```

- [ ] **Step 4: Admin cells + day detail.** After the grid builder:
```js
    const calCountByDate = {};
    (this.state.allBookings || []).forEach(b => { if (b.date && String(b.status || '').toLowerCase() !== 'cancelled') calCountByDate[b.date] = (calCountByDate[b.date] || 0) + 1; });
    const calAdminCells = calGrid.map(c => c.blank ? c : ({ ...c, count: calCountByDate[c.date] || 0, hasCount: (calCountByDate[c.date] || 0) > 0, pick: () => this.setState({ calSel: c.date }) }));
    const calAdminDay = (this.state.calSel ? (this.state.allBookings || []).filter(b => b.date === this.state.calSel) : [])
      .sort((a, b) => (a.time || '').localeCompare(b.time || ''))
      .map(b => { const c = b.coach ? this.coachById(b.coach) : null; const si = sessStatusInfo(b.status); return { time: b.time || '', name: b.name || 'Guest', program: b.program || '', coachName: c ? c.name : 'Any coach', statusLabel: si.label, statusBg: si.bg, statusFg: si.fg }; });
```

- [ ] **Step 5: Render-return bindings.** Update the bkView block (~5528-5531) to three states and add the calendar keys:
```js
      bkView: this.state.bkView || 'sessions', bkViewSessions: bkViewSessions, bkViewPasses: bkViewPasses, bkViewCalendar: bkViewCalendar,
      goSessions: goSessions, goPasses: goPasses, goCalendar: goCalendar,
      bkSessTabBg: bkViewSessions ? '#244232' : '#fffdf6', bkSessTabFg: bkViewSessions ? '#f4efe4' : '#244232',
      bkPassTabBg: bkViewPasses ? '#244232' : '#fffdf6', bkPassTabFg: bkViewPasses ? '#f4efe4' : '#244232',
      bkCalTabBg: bkViewCalendar ? '#244232' : '#fffdf6', bkCalTabFg: bkViewCalendar ? '#f4efe4' : '#244232',
      calMonthLabel: calMonthLabel, calPrev: calPrev, calNext: calNext, calSel: this.state.calSel || '', calHasSel: !!this.state.calSel, calSelLabel: calSelLabel, calDowHeads: calDowHeads,
      calAdminCells: calAdminCells, calAdminDay: calAdminDay,
```

- [ ] **Step 6: Toggle button + admin calendar markup.** In the Bookings tab, add a third toggle button after the Passes button (~index.html:1964):
```html
            <button onClick="{{ goCalendar }}" style="background:{{ bkCalTabBg }};color:{{ bkCalTabFg }};border:1px solid rgba(36,66,50,0.16);cursor:pointer;font-family:'Hanken Grotesk',sans-serif;font-size:14px;font-weight:700;padding:9px 18px;border-radius:999px;">Calendar</button>
```
Then, AFTER the `bkViewPasses` `</sc-if>` (~2007 close), add the calendar view:
```html
          <sc-if value="{{ bkViewCalendar }}" hint-placeholder-val="{{ false }}">
          <div>
            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;max-width:520px;">
              <button onClick="{{ calPrev }}" style="background:#fffdf6;border:1px solid rgba(36,66,50,0.2);cursor:pointer;font-size:18px;font-weight:800;color:#244232;width:36px;height:36px;border-radius:8px;">‹</button>
              <span style="font-size:16px;font-weight:800;color:#1b2a1f;">{{ calMonthLabel }}</span>
              <button onClick="{{ calNext }}" style="background:#fffdf6;border:1px solid rgba(36,66,50,0.2);cursor:pointer;font-size:18px;font-weight:800;color:#244232;width:36px;height:36px;border-radius:8px;">›</button>
            </div>
            <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:6px;max-width:520px;">
              <sc-for list="{{ calDowHeads }}" as="dh" hint-placeholder-count="7"><div style="text-align:center;font-family:'Spline Sans Mono',monospace;font-size:11px;color:#8a9579;padding-bottom:4px;">{{ dh }}</div></sc-for>
              <sc-for list="{{ calAdminCells }}" as="cc" hint-placeholder-count="35">
                <sc-if value="{{ cc.blank }}" hint-placeholder-val="{{ false }}"><div></div></sc-if>
                <sc-if value="{{ cc.date }}" hint-placeholder-val="{{ true }}"><button onClick="{{ cc.pick }}" style="position:relative;min-height:46px;background:{{ cc.selected ? '#e6efd6' : '#fffdf6' }};border:1px solid {{ cc.isToday ? '#7fb43f' : 'rgba(36,66,50,0.12)' }};border-radius:8px;cursor:pointer;font-family:'Hanken Grotesk',sans-serif;padding:5px;text-align:left;"><span style="font-size:13px;font-weight:700;color:#1b2a1f;">{{ cc.day }}</span><sc-if value="{{ cc.hasCount }}" hint-placeholder-val="{{ false }}"><span style="position:absolute;bottom:4px;right:5px;font-size:10.5px;font-weight:800;color:#4d7327;font-family:'Spline Sans Mono',monospace;">{{ cc.count }}</span></sc-if></button></sc-if>
              </sc-for>
            </div>
            <sc-if value="{{ calHasSel }}" hint-placeholder-val="{{ false }}">
            <div style="margin-top:16px;max-width:520px;">
              <div style="font-size:15px;font-weight:800;color:#1b2a1f;margin-bottom:10px;">{{ calSelLabel }}</div>
              <sc-if value="{{ calAdminDay }}" hint-placeholder-val="{{ true }}">
                <div style="display:flex;flex-direction:column;gap:8px;">
                  <sc-for list="{{ calAdminDay }}" as="r" hint-placeholder-count="0">
                    <div style="background:#fffdf6;border:1px solid rgba(36,66,50,0.12);border-radius:10px;padding:11px 15px;display:flex;align-items:center;gap:12px;flex-wrap:wrap;">
                      <span style="min-width:74px;font-weight:800;color:#244232;font-family:'Spline Sans Mono',monospace;font-size:13px;">{{ r.time }}</span>
                      <div style="flex:1;min-width:140px;"><div style="font-weight:700;color:#1b2a1f;">{{ r.name }}</div><div style="font-size:12.5px;color:#56664f;">{{ r.program }} · {{ r.coachName }}</div></div>
                      <span style="font-family:'Spline Sans Mono',monospace;font-size:10.5px;font-weight:700;text-transform:uppercase;background:{{ r.statusBg }};color:{{ r.statusFg }};padding:5px 11px;border-radius:999px;">{{ r.statusLabel }}</span>
                    </div>
                  </sc-for>
                </div>
              </sc-if>
            </div>
            </sc-if>
          </div>
          </sc-if>
```
Confirm the existing `bkViewSessions`/`bkViewPasses` `<sc-if>`s now read `=== 'sessions'`/`=== 'passes'` (from Step 2), so selecting Calendar hides both.

- [ ] **Step 7: Mirror.** `cp index.html "Pasig Greenpark Archery Camp.dc.html"`; `diff … && echo IDENTICAL`.

- [ ] **Step 8: Verify (admin calendar).** Build `_verify_cal.mjs`: reach the admin Bookings tab via the React fiber; patch `nowManila`; stub `allBookings` (several dates incl. some cancelled). Set `bkView:'calendar'` (or click the Calendar toggle). Assert:
  - the grid has correct leading blanks + day count for the month; today's cell is outlined; `‹`/`›` change `calMonthLabel` and clear `calSel`.
  - day cells show non-cancelled counts (cancelled excluded; 0 → no badge); clicking a day sets `calSel` and the detail lists that day's sessions sorted by time with status badges.
  - Calendar view shows while Sessions + Passes views are hidden; clicking Sessions/Passes restores them (three-state toggle).
  Run `node _verify_cal.mjs`; expected PASS, 0 real console errors. Confirm mirror IDENTICAL.

- [ ] **Step 9: Commit.**
```bash
git add index.html "Pasig Greenpark Archery Camp.dc.html"
git commit -m "Calendar T1: shared month grid + admin booking calendar in Bookings tab (Sessions|Passes|Calendar)"
```

---

### Task 2: Public availability calendar (Programs page)

**Files:**
- Modify: `index.html` — `pickCalDayPublic` method; public cells/day builders (render); render return; replace the Programs "Live calendar" iframe/placeholder (~index.html:535-544)
- Mirror: `Pasig Greenpark Archery Camp.dc.html`
- Test: `_verify_cal.mjs`

**Interfaces:**
- Consumes the shared grid from Task 1 (`calGrid`/`calMonthLabel`/`calPrev`/`calNext`/`calSel`/`calSelLabel`/`calDowHeads`/`calHasSel`), `startHours`, `capacity`, `endpoint`, `?action=availability`.
- Produces: `calPubCells`, `calPubDay`, `calAvailLoading`, `pickCalDayPublic`.

- [ ] **Step 1: Add `pickCalDayPublic`.** As a method (near other account/fetch methods):
```js
  pickCalDayPublic(date) {
    this.setState({ calSel: date, calDayAvail: [], calAvailLoading: true });
    const ep = this.endpoint(); if (!ep) { this.setState({ calAvailLoading: false }); return; }
    fetch(ep + '?action=availability&date=' + encodeURIComponent(date)).then(r => r.json()).then(res => {
      this.setState({ calDayAvail: (res && res.slots) || [], calAvailLoading: false });
    }).catch(() => this.setState({ calAvailLoading: false }));
  }
```

- [ ] **Step 2: Public cells + day detail.** In render, after the shared grid builder (Task 1 Step 3 — it's in the admin section but available to the whole render), add:
```js
    const calPubCells = calGrid.map(c => { if (c.blank) return c; const dow = new Date(c.date + 'T00:00:00').getDay(); const isOpen = this.startHours(dow).length > 0; const past = c.date < calTodayStr; return { ...c, open: isOpen && !past, closed: !isOpen, past: past, pick: () => this.pickCalDayPublic(c.date) }; });
    const calPubDay = ((this.state.calDayAvail) || []).map(s => {
      const left = (s.left != null) ? s.left : (s.full ? 0 : this.capacity());
      const st = (s.full || left <= 0) ? { label: 'Full', bg: '#f3e2e0', fg: '#b4512f' }
               : (left <= 2) ? { label: left + ' left', bg: '#fff1cf', fg: '#8a6a1f' }
               : { label: 'Open', bg: '#e6efd6', fg: '#4d7327' };
      return { time: s.time || '', statusLabel: st.label, statusBg: st.bg, statusFg: st.fg, isOpen: !(s.full || left <= 0), book: () => this.go('book') };
    });
```
Render-return: `calPubCells: calPubCells, calPubDay: calPubDay, calAvailLoading: this.state.calAvailLoading,` (the shared `calMonthLabel`/`calPrev`/`calNext`/`calSel`/`calSelLabel`/`calDowHeads`/`calHasSel` are already returned from Task 1).

- [ ] **Step 3: Replace the Programs iframe.** Replace the two `<sc-if>` blocks — `calendarConnected` (the iframe, ~535-539) and `calendarPending` (the placeholder, ~540-544) — with the public month calendar (keep the "Live calendar / Full schedule" header + the "Add to your calendar →" link above them, untouched):
```html
        <div style="border-radius:12px;border:1px solid rgba(127,180,63,0.28);background:#fffdf6;padding:clamp(16px,3vw,24px);">
          <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;">
            <button onClick="{{ calPrev }}" style="background:#f4efe4;border:1px solid rgba(36,66,50,0.2);cursor:pointer;font-size:18px;font-weight:800;color:#244232;width:36px;height:36px;border-radius:8px;">‹</button>
            <span style="font-size:16px;font-weight:800;color:#1b2a1f;">{{ calMonthLabel }}</span>
            <button onClick="{{ calNext }}" style="background:#f4efe4;border:1px solid rgba(36,66,50,0.2);cursor:pointer;font-size:18px;font-weight:800;color:#244232;width:36px;height:36px;border-radius:8px;">›</button>
          </div>
          <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:6px;">
            <sc-for list="{{ calDowHeads }}" as="dh" hint-placeholder-count="7"><div style="text-align:center;font-family:'Spline Sans Mono',monospace;font-size:11px;color:#8a9579;padding-bottom:4px;">{{ dh }}</div></sc-for>
            <sc-for list="{{ calPubCells }}" as="co" hint-placeholder-count="35">
              <sc-if value="{{ co.blank }}" hint-placeholder-val="{{ false }}"><div></div></sc-if>
              <sc-if value="{{ co.date }}" hint-placeholder-val="{{ true }}"><button onClick="{{ co.pick }}" style="position:relative;min-height:46px;background:{{ co.selected ? '#e6efd6' : '#fffdf6' }};border:1px solid {{ co.isToday ? '#7fb43f' : 'rgba(36,66,50,0.12)' }};border-radius:8px;cursor:pointer;font-family:'Hanken Grotesk',sans-serif;padding:5px;text-align:left;opacity:{{ co.past ? '0.45' : '1' }};"><span style="font-size:13px;font-weight:700;color:#1b2a1f;">{{ co.day }}</span><sc-if value="{{ co.open }}" hint-placeholder-val="{{ true }}"><span style="position:absolute;bottom:5px;right:6px;width:7px;height:7px;border-radius:50%;background:#7fb43f;"></span></sc-if></button></sc-if>
            </sc-for>
          </div>
          <sc-if value="{{ calHasSel }}" hint-placeholder-val="{{ false }}">
          <div style="margin-top:16px;">
            <div style="font-size:15px;font-weight:800;color:#1b2a1f;margin-bottom:10px;">{{ calSelLabel }}</div>
            <sc-if value="{{ calAvailLoading }}" hint-placeholder-val="{{ false }}"><div style="font-size:13px;color:#8a9579;">Checking availability…</div></sc-if>
            <div style="display:flex;flex-direction:column;gap:8px;">
              <sc-for list="{{ calPubDay }}" as="s" hint-placeholder-count="0">
                <div style="background:#f4efe4;border-radius:10px;padding:11px 15px;display:flex;align-items:center;gap:12px;">
                  <span style="min-width:74px;font-weight:800;color:#244232;font-family:'Spline Sans Mono',monospace;font-size:13px;">{{ s.time }}</span>
                  <span style="flex:1;font-family:'Spline Sans Mono',monospace;font-size:10.5px;font-weight:700;text-transform:uppercase;"><span style="background:{{ s.statusBg }};color:{{ s.statusFg }};padding:4px 11px;border-radius:999px;">{{ s.statusLabel }}</span></span>
                  <sc-if value="{{ s.isOpen }}" hint-placeholder-val="{{ true }}"><button onClick="{{ s.book }}" style="background:#7fb43f;color:#1b2a1f;border:none;cursor:pointer;font-family:'Hanken Grotesk',sans-serif;font-size:12.5px;font-weight:700;padding:7px 14px;border-radius:999px;">Book</button></sc-if>
                </div>
              </sc-for>
            </div>
          </div>
          </sc-if>
        </div>
```
Do NOT remove the "Add to your calendar →" link (the Google subscribe link ~533). The `calendarEmbedSrc`/`calendarConnected`/`calendarPending` bindings become unused by the public calendar — leave their builders (harmless) or note removal; do not break other references.

- [ ] **Step 4: Mirror.** `cp` + `diff … && echo IDENTICAL`.

- [ ] **Step 5: Verify (public calendar + privacy) + cleanup.** Extend `_verify_cal.mjs`: navigate to the Programs page (`go('programs')`); stub `fetch` so `?action=availability&date=` returns a slot set (mix of open/few/full) and FAIL the test if any `?action=bookings` request is made from the public page. Assert:
  - the old `<iframe ... calendarEmbedSrc ...>` is GONE from Programs; the month grid renders.
  - cells: open days show the green dot (per `startHours`), closed days don't, past days are dimmed (opacity), today outlined.
  - tapping an OPEN day fires exactly one `?action=availability&date=<that day>` GET; the detail lists each slot as Open / N left / Full with a Book button only on open slots; tapping Book calls `go('book')`.
  - the shared `‹`/`›` change the month; NO `?action=bookings` was ever requested.
  - Keep Task 1 (admin) assertions green. Run `node _verify_cal.mjs`; expected ALL PASS, 0 real console errors. Mirror IDENTICAL. Then delete scratch:
```bash
rm -f _verify_cal.mjs _cal*.png && rm -rf node_modules package.json package-lock.json
git status --short
```

- [ ] **Step 6: Commit.**
```bash
git add index.html "Pasig Greenpark Archery Camp.dc.html"
git commit -m "Calendar T2: public availability calendar on Programs (replaces the Google Calendar iframe; PII-free)"
```

---

## Self-review notes

- **Spec coverage:** shared month grid + nav (T1 Step 3); three-state `bkView` + admin calendar cells/detail + toggle + markup (T1); public cells (open/closed) + per-day availability fetch + detail + iframe replacement (T2); privacy enforced (T2 verify fails on any `?action=bookings`). All spec sections map to a step.
- **Reuse:** T2 reuses T1's `calGrid`/nav/`calDowHeads`/`calSel`/`calSelLabel`; only public-specific cells/day/fetch are new.
- **Three-state toggle:** `bkViewSessions`/`bkViewPasses`/`bkViewCalendar` are exact equality so exactly one Bookings sub-view shows; tab colors per state.
- **Privacy:** public uses only `?action=availability`; admin uses `allBookings`. The verify asserts no `?action=bookings` from Programs.
- **Type/name consistency:** every `{{ binding }}` in T1/T2 markup is produced in the respective render-return steps; `pickCalDayPublic` (T2) defined as a method and referenced by `calPubCells[].pick`.
- **Grid math/edge:** leading blanks from `getDay()`; `new Date(y,m,0)` days-in-month; month nav clamps Jan/Dec; selecting a day re-renders `selected`; changing month clears `calSel`/`calDayAvail`. Availability `left` may be null → derived from `capacity()`.
- **Mirror discipline:** each task ends with `cp` + `diff … && echo IDENTICAL`; scratch removed in T2 Step 5.
- **No backend/deploy:** frontend-only; merge + push when done.
